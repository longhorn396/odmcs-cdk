version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
      python: 3.7
    commands:
      - apt-get update -y
      - npm install -g aws-cdk
      - pip install -r requirements.txt
      - cdk bootstrap
  pre_build:
    commands: 
      - cdk ls
      - cdk synth *
